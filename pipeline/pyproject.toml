[project]
name = "arxiv-cleaner"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "orjson>=3.12.0",
    "pylatexenc>=2.11",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.uv]
package = false